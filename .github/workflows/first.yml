name: First workflow
on: [ push, workflow_dispatch ]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print First Step
        run: echo "First step."
      - name: Second Step Printing
        run: echo "Second step."
